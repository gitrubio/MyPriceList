package com.example.mypricelist.models

 class ProductModel(val nombre: String, val unidad: String, var cantidad: Int, val tipo: String, var imgID: Int, var id: String?="123456789") {
     //  Propiedades de la clase Pedido
     // La propiedad 'nombre' es de tipo String y se inicializa con el valor del parámetro 'nombre' del constructor
     // La propiedad 'unidad' es de tipo String y se inicializa con el valor del parámetro 'unidad' del constructor
     // La propiedad 'cantidad' es de tipo Int y se inicializa con el valor del parámetro 'cantidad' del constructor
     // La propiedad 'tipo' es de   tipo String y se inicializa con el valor del parámetro 'tipo' del constructor

     init{

     }
 }